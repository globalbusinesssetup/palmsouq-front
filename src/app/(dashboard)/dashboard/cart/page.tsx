import React from 'react';
import Cart from '../../../(default)/cart/Cart';

const Page = () => {
  return (
    <main className="container mx-auto mt-6">
      <Cart />
    </main>
  );
};

export default Page;
